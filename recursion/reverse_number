Input: n=5, arr = [1,2,3,4,5]
Output: [5,4,3,2,1]

solution:
def reverse(arr, n):
  left, right = 0, n - 1

  while left < right:
      arr[left], arr[right] = arr[right], arr[left]
      left += 1
      right -= 1

  return arr
n=int(input())
arr=list(map(int,input.split()))
print(reverse(arr,n))
